# GraphRAG 前端快速开始指南

## 环境要求

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 或 **bun** >= 1.0.0
- **后端服务** >= GraphRAG 3.0 (运行在 http://localhost:9621)

## 快速安装

### 1. 进入项目目录

```bash
cd graphrag/frontend
```

### 2. 安装依赖

**使用 npm：**
```bash
npm install
```

**使用 bun（推荐，速度更快）：**
```bash
bun install
```

### 3. 启动开发服务器

```bash
npm run dev
```

服务将启动在 http://localhost:3000

### 4. 访问应用

打开浏览器访问：http://localhost:3000

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产版本 |
| `npm run lint` | 运行代码检查 |

---

## 项目结构速览

```
graphrag/frontend/
├── src/
│   ├── api/           # API接口
│   ├── components/    # UI组件
│   ├── features/      # 功能页面
│   │   ├── documents/ # 文档管理
│   │   ├── graph/     # 知识图谱
│   │   ├── retrieval/ # 检索测试
│   │   ├── dashboard/ # 仪表板
│   │   └── login/     # 登录
│   ├── stores/        # 状态管理
│   └── lib/           # 工具函数
├── package.json
├── vite.config.ts
└── README.md
```

---

## 功能模块介绍

### 📄 文档管理
- 查看所有文档及其状态
- 批量选择和删除
- 自动刷新状态

### 🕸️ 知识图谱
- 可视化展示知识图谱
- 支持缩放、平移、拖拽
- 力导向布局

### 🔍 检索测试
- 输入查询问题
- 选择查询模式
- 流式显示结果

### 📊 仪表板
- 文档统计概览
- 系统健康状态
- 快速导航

---

## 配置说明

### 修改后端地址

编辑 `vite.config.ts`：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://your-backend:9621',  // 修改这里
      changeOrigin: true,
    },
  },
}
```

### 修改端口

编辑 `vite.config.ts`：

```typescript
server: {
  port: 8080,  // 修改为你想要的端口
}
```

---

## 开发 workflow

### 添加新页面

1. 在 `src/features/` 下创建目录
```bash
mkdir src/features/my-feature
touch src/features/my-feature/index.tsx
```

2. 编写组件代码
```tsx
export default function MyFeature() {
  return <div>My Feature</div>
}
```

3. 在 `App.tsx` 添加路由
```tsx
<Route path="/my-feature" element={<MyFeature />} />
```

4. 在 `Layout.tsx` 添加导航
```tsx
{ path: '/my-feature', label: 'My Feature', icon: Icon },
```

### 添加新组件

1. 在 `src/components/ui/` 创建文件
```bash
touch src/components/ui/my-component.tsx
```

2. 编写组件
```tsx
import { cn } from '@/lib/utils'

export function MyComponent({ className }) {
  return <div className={cn('base-class', className)}>...</div>
}
```

---

## 故障排除

### 问题1：依赖安装失败

**解决：**
```bash
# 清除缓存后重试
rm -rf node_modules package-lock.json
npm install
```

### 问题2：端口被占用

**解决：**
```bash
# 使用其他端口
npm run dev -- --port 3001
```

### 问题3：API请求失败

**检查：**
1. 后端服务是否启动
2. vite.config.ts 中的代理配置是否正确
3. 浏览器控制台是否有错误信息

### 问题4：样式不生效

**解决：**
```bash
# 重启开发服务器
npm run dev
```

---

## 构建部署

### 本地构建

```bash
npm run build
```

构建输出在 `dist/` 目录

### 部署到服务器

1. 构建项目
```bash
npm run build
```

2. 复制 `dist/` 目录到服务器

3. 配置 Nginx（参考部署文档）

---

## 技术栈版本

| 技术 | 版本 |
|------|------|
| React | 19.x |
| TypeScript | 5.6+ |
| Vite | 6.x |
| Tailwind CSS | 3.4+ |
| Zustand | 5.x |

---

## 相关文档

- [完整迁移方案](./前端迁移方案.md)
- [部署说明](./部署说明.md)
- [API接口文档](./前端迁移方案.md#API接口文档)

---

## 获取帮助

- 查看浏览器控制台错误信息
- 参考 `docs/` 目录下的文档
- 检查网络请求的响应状态

---

**提示**: 开发时建议打开浏览器开发者工具，便于调试和查看错误信息。
