# 为 operate.py 添加中文注释规格说明

## 背景与目的
`operate.py` 是 LightRAG 的核心操作模块，包含5000行代码，负责知识图谱的实体提取、关系处理、合并和查询等核心功能。由于代码量大且缺乏注释，给后续维护和重构带来困难。

## 变更内容
- 为 `operate.py` 文件的所有函数、类和方法添加中文注释
- 注释应清晰说明函数功能、参数、返回值和使用场景
- 保持现有代码逻辑不变，仅添加注释

## 影响范围
- 受影响的文件：`lightrag/operate.py`
- 不影响现有功能，仅添加文档注释

## 新增需求
### 要求：为函数添加中文注释
系统应当为 `operate.py` 中的所有函数和方法提供中文注释，注释内容包括：

#### 场景：添加函数注释
- **当** 查看 `operate.py` 中的函数时
- **应当** 看到清晰的中文功能说明
- **包括**：函数功能描述、参数说明、返回值说明

## 修改需求
### 要求：现有函数保持不变
现有代码逻辑保持完全一致，不进行任何修改

## 移除需求
无

## 验收标准
- [ ] 所有函数都有中文注释
- [ ] 代码功能保持不变
- [ ] 注释清晰易懂，符合代码逻辑
